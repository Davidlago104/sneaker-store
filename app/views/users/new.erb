<h1>Sign up here!</h1>

<%= render partial: 'layouts/error', locals: {thing: @user} %>

<%= form_for(@user, url: signup_path(@user)) do |f|%>
  <%=f.label :username %>
  <%=f.text_field :username%>

  <%= f.label :password%>
  <%=f.password_field :password%>
  <br>
  <%=f.submit "submit"%>

<% end %>
